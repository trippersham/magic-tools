{
  "$defs": {
    "DeckLine": {
      "additionalProperties": false,
      "description": "One decklist entry: a card name, a quantity, and an optional resolved\noracle_id (populated once the name is looked up against the card table).",
      "properties": {
        "card_name": {
          "description": "Card name as written in the decklist.",
          "title": "Card Name",
          "type": "string"
        },
        "oracle_id": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Resolved Scryfall oracle_id, if the name has been matched.",
          "title": "Oracle Id"
        },
        "quantity": {
          "description": "Number of copies of this card in the deck.",
          "title": "Quantity",
          "type": "integer"
        }
      },
      "required": [
        "card_name",
        "quantity"
      ],
      "title": "DeckLine",
      "type": "object"
    }
  },
  "additionalProperties": false,
  "description": "A deck boundary object: name, commander(s), optional strategy text, the\ndecklist lines, and an optional Airtable record id used as a join key.",
  "properties": {
    "airtable_record_id": {
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "description": "Airtable Decks record id (rec…), a durable join key.",
      "title": "Airtable Record Id"
    },
    "commanders": {
      "description": "Commander card name(s); empty for non-Commander formats.",
      "items": {
        "type": "string"
      },
      "title": "Commanders",
      "type": "array"
    },
    "lines": {
      "description": "Decklist entries (non-commander cards, with quantities).",
      "items": {
        "$ref": "#/$defs/DeckLine"
      },
      "title": "Lines",
      "type": "array"
    },
    "name": {
      "description": "Deck name (Airtable Decks primary field).",
      "title": "Name",
      "type": "string"
    },
    "strategy": {
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "description": "Free-text strategy (Airtable Decks.Strategy; see strategy-schema.md).",
      "title": "Strategy"
    }
  },
  "required": [
    "name"
  ],
  "title": "Deck",
  "type": "object"
}
